export interface Book {
    id: string,
    name: string,
    author: string,
    summary: string,
    coverUrl: string,
    totalPages: number,
    createdAt: Date,
    categories: string,
    ratings: string,
}